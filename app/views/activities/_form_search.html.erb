<%= form_with url: search_activities_path, method: :get, html: { class: 'mb-4', data: { search_activities_target: 'form' } } do |f| %>
  <%= f.text_field :query,
                   class: "form-control",
                   placeholder: 'Recherche par activité',
                   value: params[:query],
                   data: { search_activities_target: 'input',
                           action: 'keyup->search-activities#update' } %>
<%# end %>
<%#= form_with url: root_path, method: :get, html: { class: 'mb-4', data: { search_address_target: 'form' } } do |f| %>
  <%= f.text_field :address,
                   class: "form-control my-3",
                   placeholder: 'Recherche par ville',
                   value: params[:address],
                   data: { search_address_target: 'input',
                           action: 'keyup->search-activities#update' } %>
  <%= f.submit "Rechercher", class: "btn gradient-btn g-btn-lg" %>
<% end %>
