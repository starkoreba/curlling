<div class="container">
  <%= render "activities/show", activity: @activity %>
  <br>
  <br>
  <div class="row mt-5">
  <%= simple_form_for [@activity, @new_participation] do |f| %>
    <%= f.button :submit, "I'm in", class: "gradient-btn g-btn-lg" %>
  <% end %>
  </div>
</div>
