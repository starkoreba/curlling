<div id="message-<%= message.id %>">
  <small>
    <strong><%= message.user.nickname %> : </strong>
    <i><%= message.created_at.strftime("%a %e %b") %></i>
  </small>
  <p><%= message.content %></p>
</div>
